import React from 'react';
import PropTypes from 'prop-types';

const Column = props => {
    return (
        <td>
            Column
        </td>
    );
};

Column.propTypes = {

};

export default Column;
