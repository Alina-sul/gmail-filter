import React from 'react';
import PropTypes from 'prop-types';

const BarChart = props => {
    return (
        <div>
List
        </div>
    );
};

BarChart.propTypes = {

};

export default BarChart;
